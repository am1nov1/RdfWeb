# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# JSON
GET     /api/subject/:id                controllers.Application.subject(id: String)

# HTML
GET     /                           controllers.Application.index_html
GET     /objects/              controllers.Application.objects_html
GET     /predicates/           controllers.Application.predicates_html
GET     /subject/:id           controllers.Application.subject_html(id: String)
GET     /subjects/             controllers.Application.subjects_html

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
